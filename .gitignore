# dependencies
/node_modules

# next.js
/.next/
/out/

# misc
.DS_Store
*.pem

# debug
npm-debug.log*

# env files — never commit secrets; .env.example is the only tracked env file
.env*
!.env.example

# OAuth tokens (rotating refresh token — machine-local, never commit)
.brightspace-tokens.json

# sync write audit log (machine-local)
sync-audit.jsonl

# NOTE: course-drafts/ is intentionally TRACKED — drafts are canonical course
# content and git gives them version history and offsite backup.

# typescript
*.tsbuildinfo
next-env.d.ts
